"""Shared pytest setup.

Currently empty: each test file constructs its own fixtures inline since
they're small and the dependency graph is shallow. Kept as a marker so
pytest discovers the package layout correctly.
"""
